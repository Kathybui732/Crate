import Grid from './Grid'
import GridCell from './GridCell'

export { Grid, GridCell }

// These are UI components used for spacing out components. For example, one Grid is the header, which has two GridCell components. The first GridCell component has the Logo & a Menu with Menu Items for Men, Women, How It Works, and What's New. The second GridCell component has another Menu with Menu Items for Crates, Subscriptions, and Profile.